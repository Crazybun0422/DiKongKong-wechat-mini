openapi: 3.0.1
info:
  title: FLP 点数 APIs
  description: 管理 FLP 虚拟代币余额及日志的接口。
  version: 1.0.0
servers:
  - url: /api
security:
  - bearerAuth: []
paths:
  /api/flp/adjust:
    post:
      summary: 调整指定用户的 FLP 点数余额
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlpAdjustRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseFlpAdjustResponse'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseObject'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseObject'
  /api/flp/logs:
    get:
      summary: 分页查询 FLP 点数操作日志
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 0
          description: 页码，从 0 开始。
        - in: query
          name: size
          schema:
            type: integer
            minimum: 1
          description: 每页条数。
        - in: query
          name: featureCode
          schema:
            type: string
          description: 要过滤的用户特征码，可选。
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponsePageFlpLogResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseObject'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    FlpAdjustRequest:
      type: object
      required:
        - featureCode
        - amount
        - operation
      properties:
        featureCode:
          type: string
          description: 用户特征码。
        amount:
          type: number
          format: double
          description: 调整的点数，正数，可包含小数。
        operation:
          type: string
          description: 调整类型，取值 INCREASE 或 DECREASE。
        reason:
          type: string
          description: 调整原因，选填。
    FlpAdjustResponse:
      type: object
      properties:
        featureCode:
          type: string
        balance:
          type: number
          format: double
        amount:
          type: number
          format: double
        operation:
          type: string
    FlpLogResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        featureCode:
          type: string
        operation:
          type: string
        amount:
          type: number
          format: double
        reason:
          type: string
        createdAt:
          type: string
          format: date-time
    PageFlpLogResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/FlpLogResponse'
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
    ApiResponseFlpAdjustResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          $ref: '#/components/schemas/FlpAdjustResponse'
    ApiResponsePageFlpLogResponse:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          $ref: '#/components/schemas/PageFlpLogResponse'
    ApiResponseObject:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          nullable: true
