openapi: 3.0.3
info:
  title: DiKongKong Membership API
  version: 1.0.0
  description: 会员状态与会员充值相关接口文档
servers:
  - url: /
paths:
  /api/config/member-recharge:
    get:
      tags: [Config]
      summary: 获取会员充值配置（客户端读取）
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseMemberRechargeConfig'
    put:
      tags: [Config]
      summary: 更新会员充值配置（仅管理端）
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemberRechargeConfigRequest'
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseMemberRechargeConfig'
        '401':
          description: 未授权或非管理员
  /api/config/member-invite-reward:
    get:
      tags: [Config]
      summary: 获取会员邀请奖励配置（客户端读取）
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseMemberInviteRewardConfig'
    put:
      tags: [Config]
      summary: 更新会员邀请奖励配置（仅管理端）
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemberInviteRewardConfigRequest'
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseMemberInviteRewardConfig'
        '401':
          description: 未授权或非管理员
  /api/user/member/recharge:
    post:
      tags: [User]
      summary: 会员充值（支持微信与FLP）
      description: 支付成功后刷新会员状态和会员到期日；若该会员来自邀请关系，则每次充值都会给邀请人发放FLP奖励并生成奖励日志。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemberRechargeRequest'
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseAny'
        '400':
          description: 参数错误
        '401':
          description: 未授权
  /api/user/member/logs:
    get:
      tags: [User]
      summary: 分页获取当前用户会员日志
      description: 返回当前登录用户的会员订单日志，包含微信人民币充值与 FLP 充值记录。
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            minimum: 0
          description: 页码，从 0 开始。
        - in: query
          name: size
          schema:
            type: integer
            minimum: 1
          description: 每页数量，默认 20。
      responses:
        '200':
          description: 成功
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseAny'
        '401':
          description: 未授权
  /api/user/profile:
    get:
      tags: [User]
      summary: 获取个人资料
      description: 包含新增会员字段 `member` 和 `memberExpireDate`。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: 成功
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    ApiResponseMemberRechargeConfig:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          $ref: '#/components/schemas/MemberRechargeConfigResponse'
    ApiResponseMemberInviteRewardConfig:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          $ref: '#/components/schemas/MemberInviteRewardConfigResponse'
    ApiResponseAny:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: object
          additionalProperties:
            type: string
        data:
          description: 微信充值时返回预支付参数；FLP充值时返回扣费结果
          type: object
    MemberRechargeConfigRequest:
      type: object
      required:
        - yearlyWechatNetPrice
        - yearlyWechatListPrice
        - yearlyFlpNetPrice
        - yearlyFlpListPrice
        - monthlyWechatNetPrice
        - monthlyWechatListPrice
        - monthlyFlpNetPrice
        - monthlyFlpListPrice
        - cashPaymentEnabled
      properties:
        yearlyWechatNetPrice:
          type: number
          format: double
          description: 年度会员微信实付价（元）
        yearlyWechatListPrice:
          type: number
          format: double
          description: 年度会员微信划线价（元）
        yearlyFlpNetPrice:
          type: number
          format: double
          description: 年度会员FLP实付价（FLP）
        yearlyFlpListPrice:
          type: number
          format: double
          description: 年度会员FLP划线价（FLP）
        monthlyWechatNetPrice:
          type: number
          format: double
          description: 月度会员微信实付价（元）
        monthlyWechatListPrice:
          type: number
          format: double
          description: 月度会员微信划线价（元）
        monthlyFlpNetPrice:
          type: number
          format: double
          description: 月度会员FLP实付价（FLP）
        monthlyFlpListPrice:
          type: number
          format: double
          description: 月度会员FLP划线价（FLP）
        cashPaymentEnabled:
          type: boolean
          description: 会员现金支付是否开启
    MemberRechargeConfigResponse:
      allOf:
        - $ref: '#/components/schemas/MemberRechargeConfigRequest'
        - type: object
          properties:
            updatedAt:
              type: string
              format: date-time
              description: 配置更新时间
    MemberInviteRewardConfigRequest:
      type: object
      required: [yearlyRewardFlp, monthlyRewardFlp]
      properties:
        yearlyRewardFlp:
          type: number
          format: double
          description: 年度会员邀请奖励（默认30 FLP）
        monthlyRewardFlp:
          type: number
          format: double
          description: 月度会员邀请奖励（默认3 FLP）
    MemberInviteRewardConfigResponse:
      allOf:
        - $ref: '#/components/schemas/MemberInviteRewardConfigRequest'
        - type: object
          properties:
            updatedAt:
              type: string
              format: date-time
              description: 配置更新时间
    MemberRechargeRequest:
      type: object
      required: [cycle, paymentMode]
      properties:
        cycle:
          type: string
          enum: [MONTHLY, YEARLY]
          description: 会员周期
        paymentMode:
          type: string
          enum: [WECHAT, FLP]
          description: 支付方式
