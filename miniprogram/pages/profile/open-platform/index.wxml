<view class="open-platform-page">
  <scroll-view scroll-y="true" class="open-platform-scroll">
    <block wx:if="{{loading}}">
      <view class="open-platform-loading">加载中...</view>
    </block>
    <block wx:elif="{{error}}">
      <view class="open-platform-error">
        <text>{{error}}</text>
        <button class="open-platform-retry" bindtap="onRetryTap">重试</button>
      </view>
    </block>
    <block wx:else>
      <block wx:if="{{contentSegments.length}}">
        <block wx:for="{{contentSegments}}" wx:key="index">
          <rich-text
            wx:if="{{item.type === 'rich'}}"
            class="open-platform-content"
            nodes="{{item.nodes}}"
            space="nbsp"
            selectable="true"
            bindtap="onRichTextTap"
          />
          <image
            wx:elif="{{item.type === 'image'}}"
            class="open-platform-image"
            src="{{item.src}}"
            mode="widthFix"
            show-menu-by-longpress="true"
            data-index="{{item.index}}"
            bindtap="onImageTap"
          />
        </block>
      </block>
      <rich-text
        wx:else
        class="open-platform-content"
        nodes="{{contentNodes}}"
        space="nbsp"
        selectable="true"
        bindtap="onRichTextTap"
      />
    </block>
  </scroll-view>
</view>
